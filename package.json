{
  "name": "mailgun-event-handler",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && node dist/index.js index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint --fix 'src/**/*.ts?(x)'",
    "stop": "pkill --signal SIGINT index",
    "serverless": "serverless-bundle test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "aws-sdk": "^2.878.0",
    "serverless-bundle": "^4.2.2",
    "tslint": "^5.12.1",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "aws-xray-sdk": "^3.2.0",
    "crypto": "^1.0.1"
  }
}
